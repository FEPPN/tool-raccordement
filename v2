<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Simulateur de raccordement électrique</title>
<link rel="preconnect" href="https://fonts.cdnfonts.com">
<link rel="stylesheet" href="https://fonts.cdnfonts.com/css/avenir">
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: auto; min-height: 0; }
body { font-family: 'Avenir', 'Segoe UI', Arial, sans-serif; background: #F1F0FF; padding: 16px; line-height: 1.6; color: #1A1A2E; }
.container { max-width: 640px; margin: 0 auto; background: #ffffff; border-radius: 20px; padding: 32px 36px 36px; box-shadow: 0 12px 40px rgba(90,82,255,0.12); }
.progress-track { width: 100%; height: 5px; background: #F1F0FF; border-radius: 10px; overflow: hidden; margin-bottom: 24px; }
.progress-fill { height: 100%; width: 8%; background: #5A52FF; border-radius: 10px; transition: width 0.4s ease; }
h1 { color: #5A52FF; margin-bottom: 24px; font-size: 24px; text-align: center; font-weight: 800; letter-spacing: -0.3px; }
.question-block { display: none; animation: fadeIn 0.4s ease; }
.question-block.active { display: block; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(12px); } to { opacity: 1; transform: translateY(0); } }
.question-header { display: flex; align-items: flex-start; gap: 10px; margin-bottom: 16px; }
.question-text { font-size: 19px; color: #1A1A2E; font-weight: 700; line-height: 1.35; }
.help-icon { position: relative; display: inline-flex; align-items: center; justify-content: center; width: 24px; height: 24px; margin-top: 2px; background: #5A52FF; color: white; border-radius: 50%; font-size: 13px; font-weight: 700; cursor: pointer; flex-shrink: 0; transition: background 0.2s ease; }
.help-icon:hover { background: #4842CC; }
.help-icon:hover .tooltip, .help-icon.tooltip-open .tooltip { display: block; }
.tooltip { display: none; position: absolute; bottom: 32px; left: 50%; transform: translateX(-50%); background: #5A52FF; color: white; padding: 12px 16px; border-radius: 10px; font-size: 13px; width: 240px; z-index: 1000; line-height: 1.5; font-weight: 400; box-shadow: 0 8px 20px rgba(90,82,255,0.3); }
.tooltip::after { content: ''; position: absolute; top: 100%; left: 50%; transform: translateX(-50%); border: 7px solid transparent; border-top-color: #5A52FF; }
.note-box { background: #F6F5FF; padding: 14px 16px; border-radius: 12px; margin: 14px 0; font-size: 13.5px; border: 1px solid #E5E3FF; display: flex; gap: 8px; align-items: flex-start; }
.definition { background: #FAFAFF; border-radius: 12px; padding: 14px 16px; color: #555; font-size: 13.5px; margin: 14px 0; }
.definition strong { color: #1A1A2E; }
.options { display: flex; flex-direction: column; gap: 10px; margin-top: 18px; }
.option-btn { position: relative; background: #ffffff; border: 2px solid #E5E3FF; padding: 16px 18px 16px 46px; border-radius: 12px; cursor: pointer; transition: all 0.2s ease; font-size: 15px; font-weight: 600; text-align: left; color: #1A1A2E; min-height: 54px; font-family: inherit; width: 100%; }
.option-btn::before { content: ''; position: absolute; left: 16px; top: 50%; transform: translateY(-50%); width: 18px; height: 18px; border-radius: 50%; border: 2px solid #ACA8FF; background: #fff; transition: all 0.2s ease; }
.option-btn:hover { border-color: #ACA8FF; background: #F9F9FF; }
.option-btn:hover::before { border-color: #5A52FF; }
.input-wrap { position: relative; margin-top: 14px; }
input[type="number"] { width: 100%; padding: 16px 50px 16px 18px; border: 2px solid #E5E3FF; border-radius: 12px; font-size: 16px; font-family: inherit; color: #1A1A2E; -moz-appearance: textfield; }
input[type="number"]::-webkit-outer-spin-button, input[type="number"]::-webkit-inner-spin-button { -webkit-appearance: none; margin: 0; }
input[type="number"]:focus { outline: none; border-color: #5A52FF; box-shadow: 0 0 0 4px rgba(90,82,255,0.12); }
.input-suffix { position: absolute; right: 18px; top: 50%; transform: translateY(-50%); color: #9B97FF; font-size: 14px; font-weight: 600; pointer-events: none; }
.result-box { display: none; background: #ffffff; padding: 26px 24px; border-radius: 16px; margin-top: 8px; border: 2px solid #5A52FF; }
.result-box.show { display: block; animation: fadeIn 0.4s ease; }
.result-title { display: flex; align-items: center; gap: 8px; color: #5A52FF; font-size: 18px; font-weight: 800; margin-bottom: 18px; }
.result-title .check { width: 24px; height: 24px; border-radius: 50%; background: #5A52FF; color: #fff; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: 14px; }
.price { font-size: 30px; color: #5A52FF; font-weight: 800; margin: 6px 0 10px; letter-spacing: -0.5px; display: block; }
.price-refaction { font-size: 24px; color: #4842CC; font-weight: 800; margin: 8px 0; display: block; }
.price-ttc { font-size: 16px; color: #4842CC; font-weight: 700; margin: 6px 0 4px; display: block; }
.result-box p { font-size: 14px; color: #1A1A2E; }
.info-box { background: #F6F5FF; padding: 14px 16px; border-radius: 12px; margin-top: 14px; font-size: 13.5px; border-left: 4px solid #5A52FF; color: #1A1A2E; line-height: 1.55; }
.reset-btn { background: #5A52FF; color: white; border: none; padding: 15px 28px; border-radius: 12px; cursor: pointer; font-size: 15px; font-weight: 700; margin-top: 20px; width: 100%; transition: all 0.2s ease; font-family: inherit; }
.reset-btn:hover { background: #4842CC; transform: translateY(-1px); box-shadow: 0 6px 16px rgba(90,82,255,0.25); }
@media (max-width: 768px) {
  body { padding: 10px; }
  .container { padding: 20px 18px 24px; border-radius: 16px; }
  h1 { font-size: 20px; margin-bottom: 18px; }
  .question-text { font-size: 17px; }
  .option-btn { padding: 14px 16px 14px 42px; font-size: 14px; }
  .price { font-size: 25px; }
  .price-refaction { font-size: 20px; }
}
</style>
</head>
<body>
<div class="container">
  <div class="progress-track"><div class="progress-fill" id="progressFill"></div></div>
  <h1>Estimez le coût de votre raccordement électrique</h1>

  <div id="q-initial" class="question-block active">
    <div class="question-header">
      <div class="question-text">Connaissez-vous la Longueur du Branchement (LB) et la Longueur d'Extension nécessaire (LE) ?</div>
      <div class="help-icon" onclick="toggleTooltip(this); trackTool('click_help_tooltip')">?
        <div class="tooltip">La LB est la longueur du câble entre le réseau public et votre compteur. La LE est la longueur de réseau à créer pour atteindre votre terrain.</div>
      </div>
    </div>
    <div class="definition">
      <strong>LB (Longueur de Branchement) :</strong> Distance entre le réseau public et votre installation.<br>
      <strong>LE (Longueur d'Extension) :</strong> Longueur de réseau à créer pour desservir votre terrain.
    </div>
    <div class="note-box">⚠️ Si vous ne connaissez pas ces informations, l'estimation sera moins précise et fiable.</div>
    <div class="options">
      <button type="button" class="option-btn" onclick="startPath('A')">Non, je ne connais pas ces longueurs</button>
      <button type="button" class="option-btn" onclick="startPath('B')">Oui, je connais ces longueurs</button>
    </div>
  </div>

  <div id="qa1" class="question-block">
    <div class="question-header"><div class="question-text">Quelle est votre puissance nécessaire ?</div></div>
    <div class="options">
      <button type="button" class="option-btn" onclick="answerA1('low')">Inférieure ou égale à 36 kVA</button>
      <button type="button" class="option-btn" onclick="answerA1('high')">Supérieure à 36 kVA</button>
    </div>
  </div>

  <div id="qa2" class="question-block">
    <div class="question-header">
      <div class="question-text">Votre terrain est-il viabilisé ?</div>
      <div class="help-icon" onclick="toggleTooltip(this); trackTool('click_help_tooltip')">?
        <div class="tooltip">Un terrain viabilisé dispose déjà des réseaux d'eau, d'électricité et d'assainissement à proximité immédiate.</div>
      </div>
    </div>
    <div class="options">
      <button type="button" class="option-btn" onclick="answerA2('yes')">Oui</button>
      <button type="button" class="option-btn" onclick="answerA2('no')">Non</button>
    </div>
  </div>

  <div id="qa3" class="question-block">
    <div class="question-header"><div class="question-text">Votre rue est-elle desservie par le réseau électrique ?</div></div>
    <div class="options">
      <button type="button" class="option-btn" onclick="answerA3('full')">Ma rue est totalement desservie en électricité</button>
      <button type="button" class="option-btn" onclick="answerA3('partial')">Ma rue est partiellement desservie en électricité</button>
      <button type="button" class="option-btn" onclick="answerA3('none')">Ma rue n'est pas desservie en électricité</button>
    </div>
  </div>

  <div id="qb1" class="question-block">
    <div class="question-header"><div class="question-text">Quelle est votre puissance nécessaire ?</div></div>
    <div class="options">
      <button type="button" class="option-btn" onclick="answerB1('low')">Inférieure ou égale à 36 kVA</button>
      <button type="button" class="option-btn" onclick="answerB1('medium')">Entre 36 kVA et 250 kVA</button>
      <button type="button" class="option-btn" onclick="answerB1('high')">Supérieure à 250 kVA</button>
    </div>
  </div>

  <div id="qb2" class="question-block">
    <div class="question-header">
      <div class="question-text">Quelle est votre zone de facturation ?</div>
      <div class="help-icon" onclick="toggleTooltip(this); trackTool('click_help_tooltip')">?
        <div class="tooltip">ZFA correspond aux zones urbaines denses. ZFB correspond aux zones rurales ou moins densément peuplées.</div>
      </div>
    </div>
    <div class="options">
      <button type="button" class="option-btn" onclick="answerB2('ZFA')">ZFA (Zone urbaine)</button>
      <button type="button" class="option-btn" onclick="answerB2('ZFB')">ZFB (Zone rurale)</button>
    </div>
  </div>

  <div id="qb3" class="question-block">
    <div class="question-header"><div class="question-text">Quelle est la longueur d'extension nécessaire (en mètres) ?</div></div>
    <div class="input-wrap">
      <input type="number" id="extensionLength" placeholder="0" min="0" onchange="handleExtensionInput()">
      <span class="input-suffix">mètres</span>
    </div>
  </div>

  <div id="qb4" class="question-block">
    <div class="question-header"><div class="question-text">Quelle est la longueur du branchement (en mètres) ?</div></div>
    <div class="input-wrap">
      <input type="number" id="branchementLength" placeholder="0" min="0" onchange="calculateResultB()">
      <span class="input-suffix">mètres</span>
    </div>
  </div>

  <div id="result" class="result-box">
    <div class="result-title"><span class="check">✓</span>Estimation du coût de raccordement</div>
    <div id="resultContent"></div>
    <button type="button" class="reset-btn" onclick="handleReset()">Nouvelle estimation</button>
  </div>
</div>

<script>
var userData = { path: null, powerA: null, viabilise: null, street: null, powerB: null, zone: null, extensionLength: 0, branchementLength: 0 };

function goTo(id, pct) {
  var blocks = document.querySelectorAll('.question-block');
  for (var i = 0; i < blocks.length; i++) { blocks[i].classList.remove('active'); }
  document.getElementById('result').classList.remove('show');
  var target = document.getElementById(id);
  if (id === 'result') { target.classList.add('show'); } else { target.classList.add('active'); }
  if (typeof pct === 'number') { document.getElementById('progressFill').style.width = pct + '%'; }
  sendHeight();
}

function sendHeight() {
  setTimeout(function () {
    var height = document.body.scrollHeight + 24;
    window.parent.postMessage({ type: 'resize', height: height }, '*');
  }, 100);
}

function toggleTooltip(el) { el.classList.toggle('tooltip-open'); }

function startPath(path) {
  userData.path = path;
  var actionName = (path === 'A') ? 'select_path_unknown_lengths' : 'select_path_known_lengths';
  trackTool(actionName, { event_type: 'interaction' });
  if (path === 'A') { goTo('qa1', 25); } else { goTo('qb1', 20); }
}

function answerA1(power) {
  userData.powerA = power;
  var actionName = (power === 'low') ? 'select_power_low' : 'select_power_high';
  trackTool(actionName);
  if (power === 'high') { showResultA('devis'); } else { goTo('qa2', 50); }
}

function answerA2(viabilise) {
  userData.viabilise = viabilise;
  var actionName = (viabilise === 'yes') ? 'select_viabilisation_yes' : 'select_viabilisation_no';
  trackTool(actionName);
  if (viabilise === 'yes') { showResultA('430'); } else { goTo('qa3', 75); }
}

function answerA3(street) {
  userData.street = street;
  var actionName = 'select_street_coverage_' + street;
  trackTool(actionName);
  if (street === 'full') { showResultA('1690'); } else { showResultA('1690+'); }
}

function showResultA(type) {
  trackTool('view_result_path_A');
  var content = '';
  if (type === 'devis') {
    content = '<div class="price">Prix sur devis auprès d\'Enedis</div><p>Pour une puissance supérieure à 36 kVA, le coût du raccordement est établi sur devis personnalisé par Enedis.</p>';
  } else if (type === '430') {
    content = '<div class="price">430 € TTC</div><p>Votre terrain étant déjà viabilisé, le coût du raccordement est forfaitaire.</p>';
  } else if (type === '1690') {
    content = '<div class="price">1 690 € TTC</div><p>Coût du raccordement pour un terrain non viabilisé avec une rue totalement desservie.</p>';
  } else if (type === '1690+') {
    content = '<div class="price">1 690 € TTC + part variable</div><p>Le coût inclut une base de 1 690 € TTC plus une part variable qui dépend de la longueur de l\'extension nécessaire pour desservir votre terrain.</p>';
  }
  content += '<div class="info-box"><strong>Information importante</strong><br>Ces prix sont estimés à partir des grilles tarifaires d\'Enedis. Il est recommandé de faire un devis personnalisé auprès d\'Enedis pour connaître le prix exact de votre raccordement.</div>';
  content += '<div class="info-box"><strong>Précision importante</strong><br>Ces estimations sont issues du simulateur officiel d\'Enedis. Le montant réel du raccordement peut varier en fonction de la longueur du branchement, de la zone tarifaire (ZFA/ZFB) et de l\'extension éventuellement nécessaire. Une étude et un devis Enedis sont indispensables pour déterminer le coût exact de votre raccordement.</div>';
  document.getElementById('resultContent').innerHTML = content;
  goTo('result', 100);
}

function answerB1(power) {
  userData.powerB = power;
  var actionName = 'select_power_low';
  if (power === 'medium') actionName = 'select_power_medium';
  if (power === 'high') actionName = 'select_power_high';
  trackTool(actionName);
  if (power === 'high') { showResultB('devis'); } else { goTo('qb2', 40); }
}

function answerB2(zone) {
  userData.zone = zone;
  var actionName = (zone === 'ZFA') ? 'select_zone_ZFA' : 'select_zone_ZFB';
  trackTool(actionName);
  goTo('qb3', 60);
}

function handleExtensionInput() {
  userData.extensionLength = parseFloat(document.getElementById('extensionLength').value) || 0;
  trackTool('input_length_extension');
  if (userData.powerB === 'medium') { goTo('qb4', 80); } else { calculateResultB(); }
}

function calculateResultB() {
  if (userData.powerB === 'medium') {
    userData.branchementLength = parseFloat(document.getElementById('branchementLength').value) || 0;
    trackTool('input_length_branchement');
  }
  trackTool('view_result_path_B');
  if (userData.powerB === 'devis') { showResultB('devis'); return; }

  var HT = 0, HT_branchement = 0, HT_extension = 0;
  var LE = userData.extensionLength || 0;
  var LB = userData.branchementLength || 0;

  if (userData.powerB === 'low') {
    HT_branchement = 2340.00;
    if (LE === 0) {
      HT_extension = 0;
      HT = HT_branchement;
    } else {
      if (userData.zone === 'ZFA') { HT_extension = 2549.00 + (LE * 105.00); } else { HT_extension = 3296.20 + (LE * 138.60); }
      HT = HT_branchement + HT_extension;
    }
  } else if (userData.powerB === 'medium') {
    if (LE === 0) {
      HT_branchement = 4255.00;
      HT_extension = 0;
      HT = 4255.00;
    } else {
      HT_branchement = 4255.00 + (LB * 151.00);
      if (userData.zone === 'ZFA') { HT_extension = 2587.00 + (LE * 129.00); } else { HT_extension = 4039.20 + (LE * 198.00); }
      HT = HT_branchement + HT_extension;
    }
  }

  var HT_refaction = HT * 0.60;
  var TTC_client = HT_refaction * 1.20;
  var fmt = function (n) { return n.toLocaleString('fr-FR', { minimumFractionDigits: 2, maximumFractionDigits: 2 }); };

  var content = '<div class="price">' + fmt(HT) + ' € HT</div>' +
    '<div class="price-refaction">Après réfaction 40% : ' + fmt(HT_refaction) + ' € HT</div>' +
    '<div class="price-ttc">Montant TTC à payer : ' + fmt(TTC_client) + ' € TTC</div>' +
    '<div class="info-box"><strong>Détail des coûts (HT) :</strong><br>• Part branchement : ' + fmt(HT_branchement) + ' € HT<br>• Part extension : ' + fmt(HT_extension) + ' € HT</div>' +
    '<div class="info-box"><strong>Information importante</strong><br>Ces estimations sont issues du barème Enedis 2025 (version 7.2). Il est nécessaire de demander un devis personnalisé à Enedis pour connaître le montant exact de la prestation.</div>';

  document.getElementById('resultContent').innerHTML = content;
  goTo('result', 100);
}

function showResultB(type) {
  trackTool('view_result_path_B');
  var content = '';
  if (type === 'devis') {
    content = '<div class="price">Prix sur devis auprès d\'Enedis</div><p>Pour une puissance supérieure à 250 kVA, le raccordement s\'effectue en Haute Tension et nécessite un devis personnalisé d\'Enedis.</p><div class="info-box"><strong>Information importante</strong><br>Ces estimations sont issues du barème Enedis 2025. Il est nécessaire de demander un devis personnalisé à Enedis pour connaître le montant exact de la prestation.</div>';
  }
  document.getElementById('resultContent').innerHTML = content;
  goTo('result', 100);
}

function handleReset() {
  trackTool('click_reset_simulator', { is_cta: true });
  userData = { path: null, powerA: null, viabilise: null, street: null, powerB: null, zone: null, extensionLength: 0, branchementLength: 0 };
  document.getElementById('extensionLength').value = '';
  document.getElementById('branchementLength').value = '';
  document.getElementById('resultContent').innerHTML = '';
  goTo('q-initial', 8);
}

function trackTool(actionName, customParams) {
  customParams = customParams || {};
  var nom_du_tool = "tool-raccordement";
  var gtmEvent = "form";
  if (actionName === "tool_visible") { gtmEvent = "impression"; }
  else if (customParams.is_contact || actionName.indexOf("contact") > -1 || actionName.indexOf("telephone") > -1) { gtmEvent = "click_cta"; }
  else if (actionName.indexOf("reset") > -1) { gtmEvent = "click"; }
  var eventCategory = "engagement";
  var eventType = customParams.event_type || 'interaction';
  if (actionName === "tool_visible" || actionName.indexOf("view_result") > -1) { eventType = "impression"; }
  var payload = { event: gtmEvent, event_system: 'papernest_tool', event_component: nom_du_tool, event_category: eventCategory, event_type: eventType, event_position: 'content' };
  if (actionName !== "tool_visible") { payload.event_step = actionName; }
  if (gtmEvent === "form") {
    if (actionName.indexOf("view_result") > -1 || actionName.indexOf("tooltip") > -1 || actionName.indexOf("help") > -1) {
    } else if (actionName.indexOf("input_") === 0) {
      payload.event_form_type = "text";
    } else {
      payload.event_form_type = "select";
    }
  }
  window.parent.postMessage(payload, '*');
}

window.addEventListener('load', sendHeight);
window.addEventListener('resize', sendHeight);

document.addEventListener('DOMContentLoaded', function () {
  var toolContainer = document.querySelector('.container');
  if (toolContainer && window.IntersectionObserver) {
    var observer = new IntersectionObserver(function (entries) {
      for (var i = 0; i < entries.length; i++) {
        if (entries[i].isIntersecting) {
          trackTool('tool_visible', { event_type: 'interaction' });
          observer.unobserve(entries[i].target);
        }
      }
    }, { threshold: 0.5 });
    observer.observe(toolContainer);
  }
});
</script>
</body>
</html>
